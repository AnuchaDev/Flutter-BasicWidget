import 'package:flutter/material.dart';
import 'Tabs.dart';
import 'appbar.dart';
import 'navigator.dart';
import 'workshop.dart';
import 'Form.dart';
void main() {
  // runApp(Workshop());
  // runApp(Appbar_workshop());
  // runApp(Tabs_workshop());
  // runApp(Form_workshop());
  runApp(Navigator_workshop());

}

